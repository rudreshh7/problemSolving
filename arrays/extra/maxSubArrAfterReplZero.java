public class maxSubArrAfterReplZero{
	public static void main(String[] args) {

		// the index of 0 to be replaced to get maximum length subarray of 1's

		int[] arr = {0,1,1,0,1,1,1,0,1};

		int maxlen = 0 , maxindex = -1;
		int count  0 , inde = -1;
		for (int i = 0; i < n;i++ ) {
			if(arr[i] == 1)
				count++;
			else{
				count = i-index;
				index = i;
			}

			if(count > maxlen){
				max_len = count;
				max_index = index;
			}

			
		}

		





		
	}
}